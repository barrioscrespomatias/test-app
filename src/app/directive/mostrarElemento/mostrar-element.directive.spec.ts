import { MostrarElementDirective } from './mostrar-element.directive';

describe('MostrarElementDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarElementDirective();
    expect(directive).toBeTruthy();
  });
});
