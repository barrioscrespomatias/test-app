import { ObtenerValorDirective } from './obtener-valor.directive';

describe('ObtenerValorDirective', () => {
  it('should create an instance', () => {
    const directive = new ObtenerValorDirective();
    expect(directive).toBeTruthy();
  });
});
