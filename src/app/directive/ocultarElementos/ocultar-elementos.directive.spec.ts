import { OcultarElementosDirective } from './ocultar-elementos.directive';

describe('OcultarElementosDirective', () => {
  it('should create an instance', () => {
    const directive = new OcultarElementosDirective();
    expect(directive).toBeTruthy();
  });
});
