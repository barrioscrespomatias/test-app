import { TurnoPorEstadoPipe } from './turno-por-estado.pipe';

describe('TurnoPorEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnoPorEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
