import { FitroUsuariosPipe } from './fitro-usuarios.pipe';

describe('FitroUsuariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FitroUsuariosPipe();
    expect(pipe).toBeTruthy();
  });
});
