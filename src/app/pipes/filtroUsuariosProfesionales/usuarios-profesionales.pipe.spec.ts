import { UsuariosProfesionalesPipe } from './usuarios-profesionales.pipe';

describe('UsuariosProfesionalesPipe', () => {
  it('create an instance', () => {
    const pipe = new UsuariosProfesionalesPipe();
    expect(pipe).toBeTruthy();
  });
});
