import { FiltroTurnosPacientePipe } from './filtro-turnos-paciente.pipe';

describe('FiltroTurnosPacientePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnosPacientePipe();
    expect(pipe).toBeTruthy();
  });
});
