import { ObtenerTodosLosPacientesPipe } from './obtener-todos-los-pacientes.pipe';

describe('ObtenerTodosLosPacientesPipe', () => {
  it('create an instance', () => {
    const pipe = new ObtenerTodosLosPacientesPipe();
    expect(pipe).toBeTruthy();
  });
});
