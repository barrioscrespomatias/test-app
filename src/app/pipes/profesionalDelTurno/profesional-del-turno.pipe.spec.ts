import { ProfesionalDelTurnoPipe } from './profesional-del-turno.pipe';

describe('ProfesionalDelTurnoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfesionalDelTurnoPipe();
    expect(pipe).toBeTruthy();
  });
});
