import { FiltroUsuariosPacientesPipe } from './filtro-usuarios-pacientes.pipe';

describe('FiltroUsuariosPacientesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUsuariosPacientesPipe();
    expect(pipe).toBeTruthy();
  });
});
