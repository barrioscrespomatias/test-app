import { ObtenerPacientesPipe } from './obtener-pacientes.pipe';

describe('ObtenerPacientesPipe', () => {
  it('create an instance', () => {
    const pipe = new ObtenerPacientesPipe();
    expect(pipe).toBeTruthy();
  });
});
