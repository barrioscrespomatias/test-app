import { EspecialidadesDisponiblesPipe } from './especialidades-disponibles.pipe';

describe('EspecialidadesDisponiblesPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialidadesDisponiblesPipe();
    expect(pipe).toBeTruthy();
  });
});
